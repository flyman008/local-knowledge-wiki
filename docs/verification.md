# 交付验证记录

日期：2026-09-18。

## 本轮已验证

运行 `py -3.12 _service/scripts/smoke_public_setup.py` 通过。测试复制代码与示例到临时目录，用空模型密钥启动独立服务、随机本机端口和独立数据库，不访问生产知识库。仅测试进程覆盖 CLI 地址，正式 CLI 仍固定 8765。

- 无密钥启动，Worker 未运行。
- 首页 HTTP 返回成功、分类能力接口正常。
- 仓库示例 prepared 原件、解析稿、知识稿全部保存。
- 自动一致性检查通过，实际回执可查。
- 中文关键词检索命中。
- 成稿 archive 与待确认列表查询正常。
- usage_log 为零，无后台模型调用。
- 进程退出、临时测试目录自动清理完成。

本轮补齐 prepared CLI 的 receipt_id 输出，以支持教程中的后续查验。未修改生产服务。

## 未据此宣称通过的项目

- 全新 Windows 机器下载安装依赖；本次使用现有 Python 3.12 环境。
- 三个客户端在陌生电脑上的实际发现、调用和定时任务。
- 网页视觉/交互验收；本次仅检查首页 HTTP 返回，不是浏览器逐项验收。
- 真实模型兼容性、费用、复杂 PDF/图片解析质量。
- 生产数据备份恢复演练、服务器安全和多用户并发。

测试不等于内容事实正确。没有对全部历史 test_*.py 宣称回归通过。

## 媒体保存更新（2026-09-18）

隔离媒体测试、remediation 回归及 archive_full 17/17 通过，无真实模型调用、不使用存量库。覆盖图片像素一致与去重、清单完整性、7 天等待及取消、共享原件回执、未决事项和文件损坏拦截。音视频使用合成测试字节，只验证保留规则，不证明真实媒体解析能力。

本地服务已加载 retention-v1；浏览器实际展开“媒体保存与空间管理”，历史材料显示未提交清单、原件保持不动，布局正常。未对真实历史资料执行 stage 或清理。三端 Skill 文件同步不等于 WorkBuddy/Claude 新会话已实际调用。

## 原材料日期更新验收

日期隔离测试、quality 回归、correction 回归及无模型 onboarding smoke 均通过；Skill 结构校验通过。实际浏览器检查了日期展示、未知筛选、超过一年筛选、日期区间组合和日期排序，页面布局正常。

历史补录只改来源元数据，原文与知识正文保留；本地数据库备份及变更审计不上传公共仓库。三端安装文件已同步并比对一致，不把文件同步当成三个客户端全部实际触发验收。
